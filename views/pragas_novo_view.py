import flet as ft


def PragasNovoView(page: ft.Page):
    return ft.Column(
        [ft.Text("Cadastro de praga (Em breve)")],
        alignment=ft.MainAxisAlignment.CENTER,
        horizontal_alignment=ft.CrossAxisAlignment.CENTER,
        expand=True,
    )
